/**
 * 
 */

Object.prototype.Map = function(k, v) {
	this.key = k;
	this.value = value;
}

Utils.prototype.format = function(msg, params) {

	// TODO: Implements
	return "";
};

Alert = function(msg) {
	this.message = msg;
};

Alert.prototype.show = function(onClose) {

	// TODO: Implements
};

Alert.prototype.warning = function(onClose) {

	// TODO: Implements
};

Alert.prototype.error = function(onClose) {

	// TODO: Implements
};

Alert.prototype.ask = function(onYes, onNo) {

	// TODO: Implements
};